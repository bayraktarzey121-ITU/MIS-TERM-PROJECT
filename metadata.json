import { supabase } from './supabase';

export interface Session {
  user: any;
  access_token: string;
}

export interface AuthProvider {
  signIn(credentials: { email: string; password: string }): Promise<Session>;
  signOut(): Promise<void>;
  getSession(): Promise<Session | null>;
}

// Current Supabase Implementation
export const supabaseAuthProvider: AuthProvider = {
  async signIn({ email, password }) {
    const { data, error } = await supabase.auth.signInWithPassword({ email, password });
    if (error) throw error;
    return data as unknown as Session;
  },
  async signOut() {
    const { error } = await supabase.auth.signOut();
    if (error) throw error;
  },
  async getSession() {
    const { data: { session } } = await supabase.auth.getSession();
    return session as unknown as Session | null;
  }
};

// e-Devlet SSO Stub (Future Implementation)
/*
export const eDevletAuthProvider: AuthProvider = {
  async signIn() {
     // Redirect to e-Devlet OAuth URL
     window.location.href = `https://edevlet.gov.tr/api/v1/auth?client_id=${process.env.EDEVLET_CLIENT_ID}`;
     return {} as Session;
  },
  ...
};
*/

// Export active provider
export const authProvider = supabaseAuthProvider;
